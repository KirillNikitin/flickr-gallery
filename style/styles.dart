import 'package:flutter/material.dart';

class AppColors {
  static Color white = const Color.fromRGBO(255, 255, 255, 1); // #FFFFFF
  static Color black = const Color.fromRGBO(0, 0, 0, 1); // #000000
  static Color bgDark = const Color.fromRGBO(0, 0, 0, 0.4); // #000000 40%
}

const double smallPadding = 5.0;
const double mediumPadding = 10.0;
const double bigPadding = 20.0;
